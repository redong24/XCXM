# 哮卫 AsthmaGuard · APP 重设计计划

> 工作流：**探索 ➔ 计划 ➔ 编码 ➔ 提交**
> 本文档 = 「计划」阶段产物。**未经确认不进入编码阶段。**
> 技能：`szyj-ui-design-workflow`，track = `redesign`
> 日期：2026-09-02

---

## 0. 业务定义（8 轮问答的结论，作为全部设计决策的依据）

| 维度 | 结论 |
|---|---|
| 改造对象 | Android APP，且要**超出现有 6 个页面** |
| 使用者 | **一个人**（患者本人 或 家长），**多孩版** |
| 产品性质 | **消费级**，不是科研项目 |
| 核心承诺 | **预警优先 —— 它是个「哨兵」**。戒指全天候佩戴，按设定频率（实时 / 5 分钟 / 30 分钟）连续上传 |
| 红色预警后用户能做什么 | **A + B + D**：知道并消除 + 动作指引 + 记录这次发作 |
| 动作指引来源 | **默认通用非药物安抚措施；医生录入的行动计划（Asthma Action Plan）优先** |
| 视觉性格 | **克制的现代医疗风**（Linear / Stripe 一路），交付**能跑起来的真实产品** |
| 功能范围 | **全做**：10 个页面 + 后端改动 + 推送 |
| 推送账号 | **暂时没有 DCloud 账号** → 本地通知 + 前台服务兜底 |
| 「孩子」数据模型 | **C-2 纯档案**（无手机号 / 无密码）；成人自用时戒指直接绑账号，「我自己」是孩子列表第一项 |

**已验证**：`POST /api/users/` 传 `phone=null` 连续两次均返回 HTTP 200 → 多条 `phone=NULL` 允许存在，**C-2 模型无需改表结构**，只需放开 `UserCreate.password` 的 `min_length=6`。

---

## 1. Phase 0 · ROUTE

**Track = `redesign`**（改造既有工程，非新建）。
对应 playbook `references/05-redesign-protocol.md` —— **本沙箱不可用，见 §8 披露**。

---

## 2. Phase 1 · Design Read（一句话）

> **Design Read：** 把它读作**一款消费级、全天候在岗的医疗哨兵型移动产品 UI**，服务于**一个成人（家长或患者本人）同时看护 1–N 个孩子的哮喘风险**，语言是**冷静、高可信、3 秒内可读懂的「仪表」语言**，方向倾向**克制的现代医疗风 —— Linear / Stripe 的结构严谨性 + 临床监护仪的可读性，而不是消费级运动手环的游戏化，也不是 CGM 那种深色驾驶舱**。

三条推导：
1. **"哨兵"决定信息架构**。首页第一个问题不是"我今天数据好不好"，而是**"现在有事吗？哨兵还活着吗？"** → 首屏必须同时回答风险等级 + 数据新鲜度。
2. **"消费级 + 多孩"决定导航**。家长在 2 个孩子间切换必须是 1 次点击，不能藏在设置里。
3. **"预警优先"决定视觉预算**。全应用其余部分越安静，红色预警越刺眼。**红色是全局保留色，除真实红区外任何地方不得出现。**

---

## 3. Phase 2 · CALIBRATE（三个 dial + 基础）

基线是 `8 / 6 / 4`。本项目**全部低于基线**，这是刻意的：

```
DESIGN_VARIANCE : 3   (基线 8)
MOTION_INTENSITY: 3   (基线 6)
VISUAL_DENSITY  : 4  首页 / 6  预警详情 + 报警设置
```

**DESIGN_VARIANCE = 3** —— 这是报警仪器。凌晨 3 点，非对称和惊喜都在消耗识别速度。variance 只花在**一个地方**（§4 的 signature），其余一律规整。

**MOTION_INTENSITY = 3** —— 动画只允许三种理由：
- (a) 哨兵条标记的**呼吸脉动**＝每次成功上传脉动一次，**证明数据是活的**（这是功能，不是装饰）
- (b) 安全区 / 关注区 / 红区之间的**状态换肤过渡**
- (c) 按钮 `:active` 的触觉反馈

技能规定「若声称 MOTION > 4，页面必须真的在动」。我声称 3，因此**不会**出现视差、入场序列、滚动触发动画。

**VISUAL_DENSITY 分面声明** —— 首页 4（可瞥读优先），**预警详情与报警设置 6**。依据来自竞品实证：Sentinel CGM Monitor 的 12 张商店截图里**有一半是报警配置**（per-user 设置、Ninja Alarm 自动小睡、Night Mode 起止小时、手机 / 手表分别设定、TEST HAPTIC / TEST SOUND）。**报警类产品的密度就该堆在报警配置页**，而不是首页。这一条我们照抄结构，不照抄它的视觉。

### 基础（Foundation）

技能要求「若能映射到真实设计系统就装官方包，绝不手抄它的 CSS」。**本项目无法映射**：
- uni-app x / uvue 在 Android 端**走原生渲染**，不是浏览器
- **没有 Tailwind、没有 shadcn/ui、没有 Radix**
- **只支持 flex 布局，没有 CSS Grid**；不支持 `dvh`；不支持后代 / 组合选择器；不支持大部分伪类

因此基础是：**手工 token 层**，落在 `uni.scss`（CSS 变量 + 类）+ `styles/tokens.uts`（UTS 侧需要读取的同一批值，避免两处漂移）。**一套系统，不混第二套。**

---

## 4. Phase 2.5 · Token 计划 + 「同质化自我攻击」

### 4.1 先做自我攻击（技能强制）

技能列出 AI 设计的三种默认长相：
1. 暖米白 `#F4F1EA` + 高对比衬线 + 赤陶色点缀
2. **近黑底 + 单一酸性绿 / 朱红点缀**
3. 报纸式发丝分割线 + 零圆角 + 专栏排版

**我的第一反应正是第 2 种**（深色底 + 荧光绿），而这恰好**就是 Sentinel CGM Monitor 的长相**（黑底 + `#1E1E1E` 深灰卡 + 亮柠檬绿 + 未来感窄体）。用一个类似的 prompt 重新推导，我会落到同一个地方 → **说明它不是从这个 brief 推出来的，是默认值。必须改。**

**改了什么、为什么改：**

去技能要求的「主题自身的世界」里找锚点。这个主题的世界是**呼吸和空气**，它的器物是：峰流速仪（一根带绿 / 黄 / 红分区的刻度）、指夹血氧仪、流量-容积环、雾化器，以及——

> **哮喘行动计划卡（Asthma Action Plan card）：一张贴在冰箱上的、印着绿 / 黄 / 红三个分区的纸卡。**

这是哮喘家庭**真实拥有、并且已经会读**的实物，也是国际标准做法（GINA）。**它就是本设计的 ground truth。** 这个锚点是主题特有的，不是任何一种默认审美。

由此得到第二个反默认决定：**浅色为主，不是深色为主。**
- 家长白天用它，冷静可信；
- **凌晨的预警屏应当是全应用唯一的深色 / 高饱和界面**，这样它不可能被认错——把对比度花在真正重要的地方；
- 中国消费级健康市场里，浅色读作"医疗可信"，深色读作"黑客 / 加密货币"。

### 4.2 Color（6 个命名值 + 3 个分区色）

```
--paper       #F7F8F7   极浅冷绿白（不是米白 #F4F1EA，不是纯白）
--ink         #14181A   近黑（不是 #000000）
--ink-2       #5A6468   次级石板灰
--hairline    #E2E6E4   发丝线
--accent      #2C6E8F   克制的临床蓝青（饱和度 ~53%，唯一强调色）

分区色（源自行动计划卡，降饱和到医疗观感）
--zone-green  #1F8A5F   安全区
--zone-amber  #C9821B   关注区
--zone-red    #C43A2F   红区（全局保留）

预警换肤
--alert-bg    #2A1210   深氧化红（不是黑）
--alert-fg    #FFF4F2
```

关键约束：
- **`--accent` 与任何分区色都不同**，所以普通按钮永远不会被误读成报警。
- **Colour Consistency Lock**：`--accent` 在 10 个页面里完全一致。
- **Theme Lock**：应用只有一个主题；**唯一的例外就是 signature（§4.5），换肤本身即报警语义**。

### 4.3 Type（角色 ≥ 2）

平台诚实约束：Android 端只有系统字栈可靠（Noto Sans CJK / 思源黑体）。技能禁用 Inter / Roboto 作默认，这条按 web 假设写的；在这里的正确解法是**只为数字打包一个字体**：

| 角色 | 字体 | 理由 |
|---|---|---|
| metric（数字） | **IBM Plex Mono**，仅子集化数字 + 符号（OFL，≈15KB） | **等宽数字（tabular figures）**：心率从 98 跳到 101 时数字不会左右抖动。这是功能理由，不是审美理由 |
| body（中文正文） | 系统 CJK 字栈 | 唯一可靠、零体积、正确的中文字形 |
| utility（说明 / 时间戳） | 同 body，更小字号 + 更宽字距 | 不引入第三个字族 |

不使用衬线体。技能：衬线只在真正的编辑 / 奢侈 / 传承类项目里成立——医疗监护不是。

### 4.4 Layout（一句话 + ASCII 线框）

一句话：**每一屏从上到下回答一个递进的问题——"是谁 / 有事吗 / 数据新不新 / 现在做什么 / 过去怎样"，用发丝线分层而不是卡片堆叠，卡片只在"抬升确实表达层级"时出现。**

`flex` 唯一可用 → 严格 `flex:1` + 固定 px，**绝不用百分比数学**（保留技能 C19 的意图，虽然它的原文针对 CSS Grid）。

#### 首页（哨兵）

```
┌─────────────────────────────────┐
│ 小雨 ▾            哨兵 运行中 ● │ ← 孩子切换(1击) + 哨兵真实状态
├─────────────────────────────────┤
│                                 │
│  ███████████░░░░░░░░░░░░░░░░░░  │ ← 【签名】哨兵条 绿→黄→红 连续带
│           ▲                     │   标记=当前风险位置，每次上传呼吸一次
│  安全区                          │
│  12 秒前上传 · 每 5 分钟          │ ← 数据新鲜度，与风险同等重要
│                                 │
├─────────────────────────────────┤
│  心率 98        血氧 98%         │ ← 两个数字，等宽字，无卡片
│  ──────────    ──────────       │   仅发丝线分隔
├─────────────────────────────────┤
│  现在要做的                      │
│  保持安静 · 坐直 · 观察呼吸        │ ← 取自行动计划的当前分区
│                          查看 → │
├─────────────────────────────────┤
│  今天   无预警                   │
│  9/1    夜间血氧偏低   已处理 →  │
└─────────────────────────────────┘
```

#### 预警详情（A + B + D，全屏换肤）

```
┌═════════════════════════════════┐ ← 整屏 --alert-bg，全应用唯一深色屏
│ ⌃ 返回                    19:42 │
│                                 │
│  红区预警                        │
│  小雨 · 血氧 88%（阈值 90%）      │
│  持续 45 秒                      │
│                                 │
│  ┌───────────────────────────┐  │
│  │      我知道了 / 消除警报    │  │ ← A：唯一主按钮，最大命中区
│  └───────────────────────────┘  │
├─────────────────────────────────┤
│  1  让孩子坐直，不要躺下           │ ← B：编号步骤
│  2  按行动计划使用缓解药           │
│  3  5 分钟未缓解 → 立即就医        │
│     （医生行动计划 · 2026-08-12） │ ← 来源标注，可信度的关键
├─────────────────────────────────┤
│  记录这次发作                    │ ← D
│  严重程度  ○1 ○2 ●3 ○4          │
│  用了缓解药   是 / 否             │
│  备注 ________________          │
│  ┌───────────────────────────┐  │
│  │          保存日志          │  │
│  └───────────────────────────┘  │
└═════════════════════════════════┘
```

### 4.5 Signature（唯一的记忆点）

> **「哨兵条」（The Sentinel Bar）**
>
> 首页顶部一条横向的行动计划分区带，把当前看护对象的实时风险映射到**家长本来就认识的绿 / 黄 / 红纸卡**上。一个细标记指示当前位置，**每次成功上传脉动一次**（活性证明），下方一行发丝字给出「上次上传 12 秒前」。进入红区时，**整屏换肤**、标记锁定在红端。

它**替代**了首页原有的「三张等宽指标卡」——技能明确禁止 three-equal-feature-cards 这个排布。

**交付前移除一个配件**（技能强制）：**删掉首页的 HRV 卡**。后端 `alert_engine.hrv` 直接 `return AlertDecision(False)`，HRV 根本不参与预警，界面上常年显示 `--`，这是在消耗信任。删。

### 4.6 Finish gate 预答

> 把 logo 换成另一家公司的，这个界面还会像那家产品吗？

不会。哨兵条 + 数据新鲜度作为一等公民 + 红色全局保留 + 唯一深色屏就是报警本身，这四条捆在"哮喘家庭的行动计划卡"上，换不到别的产品身上。

---

## 5. 页面清单（10 页 = 6 改 + 4 新）

### 改造（6）

| 页面 | 现状 | 改成什么 |
|---|---|---|
| `pages/login/login` | 有 `build 09-02 18:20 解析修复版` 调试横幅 | 去横幅；网络诊断降级为安静入口；**放置责任口径声明（待 §8.3 回答）** |
| `pages/home/home` | 3 张指标卡 + 🫁 emoji 按钮 + 最近记录 | 围绕**哨兵条**重建；加孩子切换；加哨兵存活状态；加"现在要做的"；**删 HRV**；**删 emoji（preflight C21）** |
| `pages/device/device` | 扫描 / 连接 / 绑定 | 加**「这枚戒指属于谁」**（绑到哪个孩子）、电量、上传频率 |
| `pages/measure/measure` | 手动 PPG 上传，被 raw-ppg 500 阻塞 | 改为「手动加测」；**给 500 一个真实的错误态**，不是静默失败 |
| `pages/history/history` | 2 tab：预警记录 / 反馈记录 | 改为**「预警时间线 + 发作日志」**，按孩子筛选，两者通过 `alert_id` 关联 |
| `pages/profile/profile` | 头像 / 反馈 / 关于 | 改为枢纽页：孩子管理 / 行动计划 / 报警设置 / 关于 |

### 新增（4）

| 页面 | 作用 |
|---|---|
| `pages/children/children` | 孩子列表（**「我自己」为第一项**）+ 新增 / 编辑（C-2 纯档案：姓名 / 年龄 / 性别，无手机号无密码） |
| `pages/alert/detail` | **A + B + D 闭环**。全部新增页里最重要的一页 |
| `pages/plan/plan` | 行动计划。默认通用非药物安抚措施；医生录入的 AAP 优先并覆盖，且**必须标注来源与日期** |
| `pages/settings/alarm` | 报警设置：上传频率、夜间模式、震动 / 铃声、**TEST 按钮**、免打扰、每个孩子的阈值展示 |

### tabBar

4 项：**哨兵 / 戒指 / 日志 / 我**。需生成 **8 个 PNG**（选中 / 未选中 × 4）。现状是纯文字无图标。**不允许用 emoji 顶替**（C21）。

---

## 6. 后端改动清单

**前置：`/home/lsz/asthmaguard/backend-product` 不是 git 仓库 → 任何编辑前先 `tar` 备份。**

| # | 改动 | 文件 | 为什么 |
|---|---|---|---|
| B1 | 新增 `GET /api/users/my-children` → 返回"我自己 + 名下孩子" | `routers/users.py` | 现在 guardian 调 `GET /api/users/` 直接 403，多孩根本取不到列表 |
| B2 | `POST /api/users/` 放行 guardian，且**强制 `guardian_id = 当前用户`**；`UserCreate.password` 改 Optional | `routers/users.py`, `schemas.py` | C-2 纯档案需要家长自助建档，且不能允许越权指定 guardian |
| B3 | `GET /api/alerts` 增加 `user_id` 过滤 + **权限收紧** | `routers/alerts.py` | 当前无 user_id 过滤，**guardian 能看到所有人的预警 = 越权泄露**。这是安全 bug，优先级高于任何 UI |
| B4 | `feedbacks` 加 `alert_id` 列 + migration + schema + router | `models.py`, `schemas.py`, `routers/feedbacks.py` | D（记录发作）必须能挂回触发它的那条预警，否则日志无法复盘 |
| B5 | `alert_engine.py`：**年龄分档阈值** + **持续 N 秒去抖** + level 分级 | `alert_engine.py` | `HR_HIGH=140` 是成人值。儿童静息心率 1–2 岁 98–140 / 3–5 岁 80–120 / 6–11 岁 75–118 → **对孩子会持续误报**。误报一次，哨兵的可信度就归零 |
| B6 | 行动计划持久化（新表 `action_plans`） | — | **本批不做**。第一版行动计划先落 `uni.getStorage` 本地，避免单批改动过大；后端表进第二批 |
| **B7** | 新表 `push_tokens`（`user_id` / `push_clientid` / `platform` / `updated_at`） | `models.py` | 我方账号体系不是 uni-id，无法用 `user_id` 维度推送，只能用 `push_clientid` 维度 → 必须自己存映射 |
| **B8** | `POST /api/push/register`（登录态，同 user 覆盖更新） | 新 `routers/push.py` | 客户端 `onLaunch` 拿到 cid 后上报；登录/续期时重报（官方：cid 3 个月不登录失效） |
| **B9** | 预警触发后调用云函数 URL + **HMAC-SHA256 签名** | `alert_engine.py` / 新 `push_client.py` | 官方明确要求对 URL 化云函数加鉴权。裸奔 = 任何人可给你的用户推假预警，医疗预警产品不可接受 |

`_float_env` 已支持环境变量覆盖，且源码注释里已经预留了"个性化基线（L2）"，B5 与既有设计意图一致。

---

## 7. 推送方案（**已有 DCloud 账号 → 走真推送**）

> 2026-09-02 更新：用户已提供 DCloud 开发者中心账号（`414***@qq.com`）。
> 本节由「本地通知兜底」改为「uni-push 2.0 真推送」，兜底降级为第二层。

### 7.1 已核实的事实（来自官方文档，非推测）

| 事实 | 出处 | 影响 |
|---|---|---|
| **服务端不支持个推 API，只能走 DCloud 云函数** | `ask.dcloud.net.cn/article/40283` 二节原文：*"使用 uni-push 2.0，服务端不支持用个推 api 推送"* | 自建 FastAPI **不能直连**，必须经 uniCloud |
| 传统服务端（Java/PHP/**Python**）的正确做法是**云函数 URL 化**成 http 接口再调 | 同上 2.3 | FastAPI → HTTP → 云函数 → 个推 → 手机。**这是唯一合法链路** |
| **uni-app x 的标准基座不含 uni-push 模块**（摇树机制） | 同上 1.3 | 必须先写 `uni.getPushClientId` 代码，再打**自定义基座**，否则运行即报错 |
| **必须云打包**（填自己包名）才能拿到属于本应用的 cid | 同上 1.3「必须操作」 | 本地跑真机调试拿不到有效 cid |
| 扩展库依赖 **3 张 opendb 表**：`opendb-tempdata`/`opendb-device`/`uni-id-device`，正式版需手建 | 同上 2.1 | 不建表 → 云函数调用直接失败 |
| 未配厂商离线推送时，**只有 app 在线才能收到消息** | 同上 1.2 加粗原文 | **这条是哨兵的生死线，见 §7.3** |
| 未配消息分类，多数厂商**单应用单设备单日仅 2 条** | 同上 2.4 | 不配分类 → 第 3 次预警就静默丢失 |
| 华为离线推送必须在华为平台填 **SHA256 证书指纹**，且与云打包证书一致 | 同上 1.2 | 需要固定签名证书，不能用临时证书 |
| vivo / 小米 / OPPO(正式) **必须上架应用商店**才能用离线厂商推送 | 同上 1.2 | 上架前这些机型只能在线推 + 兜底 |
| 华为/荣耀/魅族/OPPO(测试) **不要求上架** | 同上 1.2 | 开发期可先在这几个品牌验证离线链路 |
| `push_clientid` 3 个月不登录会失效 | `uni-cloud-push/api.html` 注意项 | 需要在登录/续期时刷新 cid 映射 |
| 单次 `sendMessage` 最多 500 设备；多客户端推送 200 万次/天 | 同上频次限制 | 本产品量级完全够用 |

### 7.2 appid ✅ 已签发并写入（2026-09-02）

用户已在开发者中心创建应用，平台签发：

| 项 | 值 |
|---|---|
| 应用名称 | `Asthma Guard` |
| 创建时间 | 2026/09/02 19:33:41 |
| **Appid** | **`__UNI__9E0A6C2`** |
| 应用类型 | **`uni-app x`** ✅ 与项目框架一致 |
| 所有者 | `414***@qq.com`（个人） |
| uni-push 1.0 / 2.0 | **均「未开通」** ← 待办 |

已改 `manifest.json`：`__UNI__ASTHMAGUARD`（手写伪造）→ `__UNI__9E0A6C2`。
格式自校验通过：`__UNI__` + 7 位数字，与官方形态一致。

**应用类型订正过程**：首次创建时类型为 `uni-app`（appid `__UNI__2868158`），用户随后重建为 `uni-app x`（appid `__UNI__9E0A6C2`）。**以 `__UNI__9E0A6C2` 为准，旧 appid 作废。**

appid 逐字符核准（两次独立读图交叉验证，因 `0/O`、`6/G`、`2/Z`、`8/B` 易混）：

```
__UNI__ 9  E  0  A  6  C  2
        │  │  │  │  │  │  └─ 数字 2（非字母 Z）
        │  │  │  │  │  └──── 字母 C
        │  │  │  │  └─────── 数字 6（非字母 G）
        │  │  │  └────────── 字母 A
        │  │  └───────────── 数字 0（零，非字母 O）
        │  └──────────────── 字母 E
        └─────────────────── 数字 9
```

已通过正则 `^__UNI__[0-9A-F]{7}$` 校验，全大写无小写字符。

⚠️ **appid 一旦定下不要再「重新获取」**：官方问答区有真实案例 —— 重新获取后 appid 变更，导致**已购插件无法关联**，且旧 appid 云打包会报「manifest.json 中的 AppID 无效」，形成两头不通的死局。本项目已用市场插件（rw 蓝牙，market id 27471），**必须锁定 `__UNI__9E0A6C2`**。

### 7.2.1 下一步（在 DCloud 侧，我无法代做）

1. 侧栏 `uni-push` → 为 `__UNI__9E0A6C2` **开通 uni-push 2.0**（当前显示"未开通"，不开通则后续全部无效）
2. 创建 uniCloud 服务空间（阿里云/腾讯云均可）
3. **手建 3 张 opendb 表**：`opendb-tempdata`、`opendb-device`、`uni-id-device`
   —— 官方：正式版 uniCloud 不会自动创建，缺表则云函数调用直接失败
4. 部署云函数 → **URL 化并设置 path**（官方特别标注"云函数一定要设置 path"）
5. 把 URL 化后的地址 + 共享密钥交给我，我接进 FastAPI（B9）

### 7.2.2 ⚠️ 对本计划的一处修正：uni-app x 不能手动勾 Push 模块

原 §7.6 写的「`manifest.json` 勾选 Push 模块」**对 uni-app x 不成立**。

官方 `uni-app-x/manifest`：*"uni-app x 不再提供内置模块选择，而是提供了摇树机制自动选择内置模块"*。

正确做法（官方接入指南 1.3 节「uni-app x 框架的 App 端」）：

1. **先在代码里写 `uni.getPushClientId`** —— 摇树机制靠静态扫描代码来决定是否打入 push 模块
2. 此时用标准基座运行**会报错，属预期现象**，不要慌
3. 【发行】→【原生 App-云打包】→ 勾选**【制作自定义调试基座】**
4. 【运行】→【运行到 Android App 基座】→ 勾选**【使用自定义基座运行】**
5. 之后 push API 才可用

**结论**：`modules` 里不加 `Push`，改为在 `App.uvue` 写入 push API 代码来触发摇树。已在 C7 批次落实。

**已完成的 manifest 改动**（本轮）：appid 替换 + 5 项通知/后台权限
`POST_NOTIFICATIONS`、`FOREGROUND_SERVICE`、`FOREGROUND_SERVICE_HEALTH`、`WAKE_LOCK`、`RECEIVE_BOOT_COMPLETED`（权限总数 10 → 15，JSON 已校验合法）。

### 7.3 哨兵的生死线：离线推送

这一条我必须单独讲，因为它直接决定产品承诺能不能成立。

> 官方原文加粗：**「若未完成开通离线厂商推送，只有在 app 在线时才能收到消息」**

「app 在线」= 应用在前台运行。而**哮喘发作最危险的时段是凌晨 2–5 点**，那时手机锁屏、app 进程大概率已被系统回收 —— **正是"不在线"的状态**。

也就是说：

- **只开在线推送 = 哨兵只在你盯着屏幕时值班。** 这等于没有哨兵。
- **离线厂商推送是本产品的必选项，不是优化项。**

所以推送不是一个「加个功能」的事，它有一条完整的前置链：

```
创建应用拿真 appid
   ↓
开通 uni-push 2.0
   ↓
生成固定签名证书（.keystore）——  华为要 SHA256 指纹且必须与云打包证书一致
   ↓
逐厂商配置离线推送（华为/荣耀/小米/OPPO/vivo）
   ↓
申请消息分类（否则单日 2 条上限，第 3 次预警静默丢失）
   ↓
云打包（自定义基座）→ 拿到真 cid → 查 Device Token 验证离线链路
```

开发期可先用**华为 / 荣耀 / 魅族 / OPPO 测试环境**（不要求上架）打通并验证；**小米 / vivo / OPPO 正式**要等上架应用商店。

### 7.4 消息分类必须申请（否则哨兵会被厂商静音）

默认按「营销类」限流 → **单应用单设备单日 2 条**。一个哨兵产品一天被限 2 条预警是荒谬的。

按官方 `options` 规范，预警消息应声明为**服务与通讯类**：

```jsonc
"category": { "harmony": "EXPRESS", "huawei": "EXPRESS", "vivo": "ORDER" },
"options": { "android": {
    "HO": { "/android/notification/importance": "NORMAL" },  // 服务与通讯类
    "XM": { "/extra.channel_id": "<小米申请的渠道id>" },        // 不传会被小米拦截
    "OP": { "/category": "<Opush类别>", "/notify_level": 16 }  // 通知栏+锁屏+横幅+震动+铃声
}}
```

`notify_level: 16`（通知栏+锁屏+横幅+震动+铃声）对红区预警是必需的 —— 静音的哨兵没有意义。

### 7.5 架构：FastAPI 怎么发出这条推送

```
戒指数据 → FastAPI /api/data/hardware-result
                ↓
          alert_engine 判定（年龄分档 + 去抖，见 B5）
                ↓ 触发红区
          写 alerts 表
                ↓
          POST 云函数 URL（HMAC 签名，见下）
                ↓
          uniCloud 云函数 asthma-push
                ↓ uniPush.sendMessage({ push_clientid, category, options, payload })
          个推 + 厂商通道
                ↓
          家长手机通知栏（锁屏 + 横幅 + 铃声）
                ↓ 点击
          uni.onPushMessage type='click' → 跳 /pages/alert/detail?id=xxx
```

**cid 映射**：本产品用**自建 uni-id 体系之外**的账号（我们自己的 `users` 表），所以不能用 `user_id` 维度推送，只能用 `push_clientid` 维度。因此需要：

| 改动 | 说明 |
|---|---|
| **B7（新增）** `devices` 表或新表存 `push_clientid` | `App.uvue onLaunch` 拿到 cid 后调 `POST /api/push/register` 上报，与我方 `user_id` 建映射 |
| **B8（新增）** `POST /api/push/register` | 入参 `{push_clientid, platform}`，需登录态；同一 user 覆盖更新 |
| 登录/token 续期时重新上报 | 官方：cid 3 个月不登录会失效 |

**云函数鉴权**：官方明确 *"需要对云函数 url 增加安全通讯鉴权"*。URL 化后的地址是公网可访问的，裸奔等于任何人都能给你的用户推假预警 —— **对一个医疗预警产品这是不可接受的**。方案：FastAPI 侧用共享密钥做 HMAC-SHA256 签名（body + 时间戳），云函数内校验，时间戳超 5 分钟拒绝。

### 7.6 第二层：本地通知兜底（保留，不删除）

即使真推送全部打通，仍保留兜底，因为存在真实的失效场景：**用户未授权通知权限、厂商通道被系统冻结、无网络、cid 失效**。

1. 通知权限已补进 `manifest.json`（见 §7.2.2；**注意 uni-app x 不勾 Push 模块，靠摇树**）
2. 用 `uni.getChannelManager().setPushChannel({ channelId:'asthma_alert', importance:4, enableLights:true, enableVibration:true, lockscreenVisibility:1 })` 建**高优先级渠道**
3. app 在线时按用户设定频率拉 `/api/alerts?user_id=&status=待处理`，命中则 `uni.createPushMessage` 建本地通知
4. `force_notification: true` 让在线消息也自动进通知栏

### 7.7 UI 契约不变 —— 但现在它能说出更准确的话

首页那行「哨兵」状态从**两态变三态**，因为现在能真实区分了：

| 状态 | 含义 | 判定依据 |
|---|---|---|
| **哨兵在岗** ● 绿 | 离线推送链路通 | cid 有效 + Device Token 存在 + 通知权限已授 |
| **仅前台值守** ◐ 黄 | 只有在线推送 | cid 有效但无 Device Token（厂商离线未配/未上架） |
| **哨兵已离岗** ○ 灰 | 收不到预警 | 无 cid / 通知权限被拒 / 无网络 |

> 这条原则不因有了账号而改变：**一个哨兵产品最不能做的事，就是在自己不在岗的时候假装在岗。**
> 现在有了 Device Token 这个可查的事实依据，「在岗」不再是一句安慰话，而是**可验证的状态**。黄态必须明确告诉用户「锁屏后可能收不到」，并给出修复入口。

---

## 8. 必须先披露的三件事

### 8.1 技能脚本在本沙箱不可用（第 3 次确认）

- `activate_ai_developer_skill` 返回 `files_on_disk: false`
- `/mnt/skills/` 目录不存在
- SKILL.md 里的 `$SKILL_DIR` 写死为 Windows 路径 `C:\Users\HHF\.workbuddy\skills\...`

**后果**：
- `scripts/search.py`（3.1MB CSV 知识库设计系统生成器）**跑不了** → **Phase 3 跳过**。技能明令 *"never invent design-system data"*，所以我**不会伪造**它的输出。§4 的每一个 token 都由 Phase 2.5 自己推导，并且我把理由全部写在上面，供你逐条否决。
- `scripts/preflight.py`（22 项机械检查）**跑不了** → **Phase 5 改为手工逐条过 22 条**，交付时列出每条的结论。
- 9 个 `references/*.md`（含本 track 的 `05-redesign-protocol.md`、`02-directives.md`、`03-ai-tells.md`）**读不到** → 我只能依据 SKILL.md 里的方法论摘要。

**补救**：把技能目录打包上传，我就能跑真的生成器和真的 preflight。

### 8.2 uvue 与技能指令的硬冲突

技能的检查项按 Web 假设编写，部分在 uni-app x 原生渲染下不成立：

| 技能条目 | 冲突 | 我的处理 |
|---|---|---|
| 「CSS Grid，绝不用 flex 百分比数学」 | uvue **没有 CSS Grid** | 保留意图：`flex:1` + 固定 px，零百分比数学 |
| `min-h-[100dvh]`，禁 `h-screen`（C02） | uvue 无 `dvh` | 用页面级容器，不写视口单位 |
| 禁 Inter / Roboto（C03） | Android 系统字栈就是 Roboto / Noto | 中文本来就走 Noto CJK；数字改用打包的 IBM Plex Mono |
| Tailwind / shadcn / Radix 官方包 | 三者都不可用 | 手工 token 层，一套系统，不混第二套 |

交付时会逐条说明哪几项因平台不适用、哪几项已满足。

### 8.3 还有一个问题没答 —— 责任口径

这是最后一个业务问题，它影响的不是审美而是**文案与合规**：

> 产品对外的公开立场是
> **(1) 医疗器械 / 辅助诊断**，还是
> **(2) 健康参考，不构成医疗建议**？

它决定：
- 登录页 / 关于页免责声明的有无与措辞
- 预警详情第 3 步的强度：**「立即就医」**（口径 1）vs **「如症状持续请及时就医」**（口径 2）
- 全应用能否出现「诊断 / 确诊 / 病情评估」这类字样
- 行动计划页是否必须强制标注「本内容由您的医生录入」而非由 APP 提供

**至少影响 10 个页面里的 6 处文案。** 编码阶段开始前需要你的答案。

---

## 9. 待清理 / 已知遗留

| 项 | 说明 | 处理 |
|---|---|---|
| 测试污染 | 生产库 `users` 表被我建了 id=7「探测孩A」、id=8「探测孩B」（验证 C-2 可行性的副作用） | **删除**，或改名为你要的第一个真实孩子档案 —— 请指示 |
| `POST /api/data/raw-ppg` 500 | 既有后端 bug，两个通道均可复现，阻塞手动检测上传 | 排到最后一批 |
| 后端无版本控制 | `/home/lsz/asthmaguard/backend-product` 非 git 仓库 | 编辑前 `tar` 备份，备份名 `backend-product_YYYY-MM-DD.tar.gz` |

---

## 10. 分批 commit 计划

每批：commit → `git fetch origin main` → rebase → squash → `push -f` → 更新 PR #1 → 交付文件给你。

| 批 | 内容 | 类型 |
|---|---|---|
| **C1** | 本计划文档 | `docs` |
| **C2** | token 层（`uni.scss` + `styles/tokens.uts`）、8 个 tabBar PNG、`pages.json` / `manifest.json` 骨架（4 个新页占位 + 通知权限） | `feat(design)` |
| **C3** | 后端 B1–B3（多孩 API + **越权泄露修复**）+ tar 备份 | `fix(backend)` ← 含安全修复，优先 |
| **C4** | 孩子管理页 + 首页围绕哨兵条重建 | `feat(app)` |
| **C5** | 预警详情 A+B+D + 行动计划页 + 后端 B4（`alert_id`） | `feat(app)` |
| **C6** | 报警设置页 + 后端 B5（年龄分档 + 去抖） | `feat(app)` |
| **C7** | **真推送**：客户端 `getPushClientId` / `onPushMessage` / 高优先级渠道 + 后端 B7–B9 + 云函数代码 + 本地通知兜底 | `feat(push)` |
| **C8** | 其余 4 页重设计 + 手工 pre-flight 22 条逐条修复 | `refactor(app)` |

**C7 依赖你在 DCloud 侧的操作**（创建应用拿真 appid、开通 uni-push、建 3 张 opendb 表、云函数 URL 化、逐厂商配离线、申请消息分类、云打包自定义基座）。代码我可以先全部写好并留出配置位，但**联调必须等这些前置完成**。

**C3 的顺序是刻意的**：`GET /api/alerts` 的越权泄露是安全缺陷，先于任何界面工作。

---

## 11. 需要你确认的 5 件事

1. **整体计划是否通行？** 特别是 §4.1 的反默认决定 —— **浅色为主 + 唯一深色屏留给预警**（我否掉了自己第一反应的深色 + 荧光绿方案，因为那正是 Sentinel CGM 的长相）。
2. **责任口径**：医疗器械 / 辅助诊断，还是 健康参考不构成医疗建议？（§8.3）
3. **测试用户 id=7 / id=8**：删除，还是改成真实孩子档案？（§9）
4. **是否上传技能目录**，让我能跑真的 `search.py` 与 `preflight.py`？（§8.1；不上传我就按 §4 自推 token + 手工过 22 条）
5. **推送做到哪一层？**（§7）你已有账号，但 appid 是手写的、应用列表为 0，需要先创建应用拿真 appid。更关键的是**离线厂商推送**：不配它，哨兵只在 app 前台运行时才收得到消息 —— 而凌晨锁屏正是最危险的时段。这一串配置（固定签名证书、华为 SHA256 指纹、逐厂商开通、消息分类申请、云打包）**我无法代做**。选 (a) 全链路离线推送 / (b) 先只做在线推送 + 本地通知兜底，首页显示黄色「仅前台值守」/ (c) 先跳过推送，做完 UI 再回来。

确认后我从 **C2** 开始编码。
